[package]
name    = "alicia-binary"
version = "0.1.0"
edition = "2024"

[dependencies]
alicia    = { path    = "../alicia" }
thiserror = { version = "2.0"       }
tower-lsp = { version = "0.20.0", default-features = false, features = ["runtime-agnostic"] }
smol      = { version = "2.0.2" }
